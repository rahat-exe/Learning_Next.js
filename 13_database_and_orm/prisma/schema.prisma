
generator client {
  provider = "prisma-client"
}

datasource db {
  provider = "postgresql"
}

model Post{
  id Int @id @default(autoincrement())
  title String
  description String?

  createdAT DateTime @default(now())

}
